<h1>All Articles</h1>
<ul id="articles">
  <% @articles.each do |article| %>
    <li><%= link_to article.title, article_path(article), class: 'article-title' %></li>
  <% end %>
  <p><%= link_to "Create New Article", new_article_path, class: 'new-article' %></p>
</ul>
